"""
Transform layer (silver).

Provides PatientDataTransformer, which cleans raw patient tables and
engineers the derived features (age, BMI, risk flags, etc.) used by the
risk-scoring model in the load layer.
"""
